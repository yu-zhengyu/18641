There are every file about Part B routine. In this routine, we use serval setting to test the routine and output serval output files. The setting txt file has explain what we set in routine, such as the make of car, and driver license number and so on, and they would has their own output txt file. For example, the result show on testoutput1.txt file just the result from testsetting1.txt.

Here are some information about the file:

setting.txt		// The routine condition setting, similar as below.

test_output.txt		// The result generated by part B routine, similar as below.

ClassDiagrams-partb	// The class diagram about part B routine

PartB			// The sources code about part B routine